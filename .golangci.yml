version: "2"

linters:
  default: standard
  enable:
    - goconst
    - misspell
    - nakedret
    - unconvert
  exclusions:
    rules:
      - path: _test\.go
        linters:
          - goconst
    paths:
      - schema.go
      - pulumiManifest.go

run:
  timeout: 20m
